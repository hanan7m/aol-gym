# اختبار الصلاحيات والعمليات

## الاختبار المحلي

شغّل الاختبارات غير المتصلة بقاعدة البيانات:

```powershell
node --test tests/role-workflows.test.js
```

هذه الاختبارات لا تنشئ أو تعدل بيانات في Supabase. وهي تتحقق من عزل تذاكر العميلة، منع المدربة غير المسندة من الإدخال، استدعاء الحجز الذري، وسياسات الحماية المطلوبة.

## اختبار Supabase التجريبي

طبّق ترحيلي SQL بالترتيب التالي قبل البدء:

1. `supabase/migrations/20260826_security_hardening.sql`
2. `supabase/migrations/20260826_atomic_private_slot_booking.sql`
3. `supabase/migrations/20260826_operational_features.sql`

أنشئ ثلاثة حسابات تجريبية فقط: عميلة، مدربة، وإدارة. ثم أنشئ إسناداً واحداً للعميلة للمدربة في جدول `trainer_client_assignments`.

| الدور | العمليات التي يجب التحقق منها | النتيجة المتوقعة |
|---|---|---|
| العميلة | تسجيل الدخول، استكمال الملف، حجز وإلغاء كلاس، حجز موعد خاص، فتح تذكرة، طلب InBody، تعديل الملف، الحضور، التقييم، طلب تحويل مسار | لا ترى إلا حجوزاتها وقياساتها وبرامجها وتذاكرها |
| المدربة | عرض المتدربات المسندات، إضافة قياس InBody، إضافة برنامج، متابعة الطلب المعلق | لا ترى أو تعدل أي عميلة غير مسندة لها |
| الإدارة | إدارة الكلاسات، العروض، الفريق، الرد على التذاكر | ترى التذاكر كافة وتدير الكتالوج فقط |

## ملاحظة الحضور

زر الحضور يسجل دخولاً أو خروجاً بالتناوب في قاعدة البيانات. الرمز المعروض في صفحة الحضور يحتاج في مرحلة لاحقة إلى قارئ QR فعلي عند البوابة؛ لا يغني الزر عن تكامل جهاز المسح.
